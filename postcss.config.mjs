const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
 /*  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-inter)'],
        sport: ['var(--font-kanit)'],
      },
    },
  }, */
};

export default config;
